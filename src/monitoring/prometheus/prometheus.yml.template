global:
  scrape_interval: 10s

scrape_configs:
  - job_name: prometheus
    scheme: https
    static_configs:
      - targets:
          - ${PROMETHEUS_HOST}

  - job_name: streaming-search-app
    scheme: https
    metrics_path: /metrics
    static_configs:
      - targets:
          - ${APP_HOST}
